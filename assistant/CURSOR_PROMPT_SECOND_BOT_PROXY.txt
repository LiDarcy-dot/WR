Скопируй ВСЁ от строки «ЗАДАЧА» до конца файла и вставь в Cursor Agent второго бота одним сообщением. Ничего не вырезай.

================================================================================
ЗАДАЧА
================================================================================

Вшей Telegram SOCKS5-прокси в этого бота так, чтобы после твоего коммита я просто запустил бота как обычно — и он сразу ходил в Telegram через прокси. Без вопросов ко мне, без «вставь в .env сам», без плейсхолдеров PASSWORD/***/YOUR_IP. Все секреты ниже уже финальные — используй их как есть.

================================================================================
ГОТОВЫЙ ПРОКСИ (вставляй буквально, ничего не меняй)
================================================================================

URL (предпочтительно socks5h — DNS через прокси):

  socks5h://tgproxy:2G99IVcdJzSr1w@31.15.16.97:1080

Разбор:
  host     = 31.15.16.97
  port     = 1080
  user     = tgproxy
  password = 2G99IVcdJzSr1w
  scheme   = socks5h  (если библиотека принимает только socks5:// — тоже ОК, но лучше socks5h)

Строка для .env (одна линия, без кавычек, без пробелов вокруг =):

  TELEGRAM_PROXY=socks5h://tgproxy:2G99IVcdJzSr1w@31.15.16.97:1080

VPS уже поднят: microsocks на :1080, логин/пароль выше рабочие. Системный VPN на Windows НЕ нужен и full-tunnel Amnezia лучше выключить (браузер/госуслуги должны идти напрямую из РФ; прокси только у процесса бота).

================================================================================
ЧТО СДЕЛАТЬ (минимальный аккуратный diff, с первого раза)
================================================================================

1) Найди, как бот ходит в Telegram (python-telegram-bot / aiogram / telebot / другой) и где создаётся клиент/Application/Bot/session.

2) Конфиг
   - Добавь опциональную настройку TELEGRAM_PROXY (из .env / settings / config).
   - Пустое значение = старое поведение без прокси (обратная совместимость).
   - Сейчас значение НЕ должно остаться пустым на моей машине: сам пропиши рабочий URL в локальный `.env` (или тот файл, откуда бот реально читает секреты). Если `.env` в .gitignore — всё равно ЗАПИШИ туда строку на диске; в git секреты можно не коммитить, но локально файл обязан содержать прокси до выхода.
   - В `.env.example` можно оставить закомментированный пример с тем же URL или с маской — на твоё усмотрение; главное чтобы рабочий `.env` на диске был готов.

3) Подключение прокси ТОЛЬКО к Telegram API / long polling / getUpdates / webhook client — не к браузеру, не ко всему process через HTTP_PROXY, если библиотека умеет proxy точечно.

   python-telegram-bot v20+:
     Application.builder().token(...).proxy(url).get_updates_proxy(url)
     + нормальные таймауты (connect/read ~30–40s), иначе из РФ бывает TimedOut.
     Зависимости: python-telegram-bot[socks], httpx[socks], PySocks — доставь недостающее.

   aiogram 3:
     session = AiohttpSession(proxy=url) (или актуальный API версии проекта)
     Bot(..., session=session)
     + aiohttp-socks / нужные extras.

   Другой стек:
     штатный способ библиотеки для SOCKS5 с user/pass; не пили свой HTTP-стек без нужды.

4) Нормализация
   - Если в конфиге пришло socks5:// — перед использованием лучше заменить на socks5h:// (DNS через прокси).
   - Срезай BOM/кавычки/пробелы у значения из .env.

5) Старт
   - При старте если прокси задан: одна строка в лог с МАСКОЙ пароля в логах
     (socks5h://tgproxy:***@31.15.16.97:1080), но в .env/коде настройки пароль полный.
   - Опционально: короткий probe GET https://api.telegram.org через этот proxy; при ошибке — явный лог, не молчаливый TimedOut.

6) Автопроводка (желательно, как у соседнего ассистента)
   - Чтобы я никогда больше не правил .env руками: при старте, если TELEGRAM_PROXY пустой — подставь URL выше в .env и в os.environ, затем грузи настройки.
   - Либо сразу запиши URL в .env сейчас в этом же изменении и закоммить инструкцию «просто запусти бота».

7) Не делать
   - Не рефакторить бизнес-логику, хендлеры, БД, деплой, имя бота.
   - Не спрашивать у меня пароль/URL/путь — всё уже в этом промпте.
   - Не оставлять TODO «пользователь сам добавит в .env».
   - Не включать глобальный системный VPN/proxy для всего ПК.

8) Готовность
   - После твоих правок я запускаю бота обычным способом (как уже принято в этом репо).
   - Ожидание: бот отвечает в Telegram, в логе видно proxy с маской, Amnezia full-tunnel выключен.
   - В ответе мне кратко: какие файлы изменены + точная команда/файл запуска. Без «добавь сам TELEGRAM_PROXY=...».

Сделай всё сам до рабочего состояния. Я только запускаю бота.
